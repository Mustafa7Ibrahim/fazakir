import 'package:flutter/material.dart';

class PrayerSettings extends StatelessWidget {
  const PrayerSettings({Key? key}) : super(key: key);

  static const routeName = '/prayer_settings';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
